# Estado en ejecución y registros (se generan solos)
escaleta.json
brainstorm.log
bs-recv*.log

# Empaquetado generado por empaquetar.ps1
dist/

# Credenciales de red (NO subir al repo público; sí van en el paquete local)
red-credenciales.json
